# Current state

[← Project Context](../PROJECT_CONTEXT.md)

- **Done & working:**
  - 5 REST skills in `additional-skills/rest/`: `mariadb-rest-service-{create,update-endpoints,authorization,show,drop}`.
  - MSM skills in `additional-skills/schema-management/`: overview `mariadb-schema-management` + `mariadb-schema-management-{create,develop,release,deploy}`. `mariadb-schema-create-script` moved to `additional-skills/sql/`.
  - `additional-skills/` reorganized into `sql/`/`rest/`/`schema-management/`; `sync-skills.sh` updated for per-subfolder selection and re-run. Counts (**76** dev skills as of 26.9.1; additional=12: 5 rest + 5 MSM + schema-create-script + `mariadb-migrator` in its own `migrator/` subfolder); sql = 47 (additional=1: only schema-create-script). Manifests match disk. **Upstream split each connector skill in two** — `mariadb-connector-<x>-install` + `-usage` for c/cpp/j/nodejs/odbc/python/r2dbc (7 → 14, hence 68 → 75); dev-only, so `sql` is unchanged.
  - **Unified test runner**: repo-root `run_tests.py` + `pytest-coverage.ini` + `.coveragerc`, plus `scripts` in the root `package.json` (`npm test`, `test:static|db|eval|e2e`, `test:shell`). Runs every suite with `mariadb-shell --pym pytest` (the shell's Python) like `mcp_plugin/run_tests.py`, with coverage appended into one report. Verified: `-m static` → 591 pass ×3; `-m e2e` selects only claude (12) and the others report "no tests selected" (exit 5, not a failure).
  - **db tier now self-provisions a sandbox** (no more "MariaDB unreachable at 127.0.0.1:3306" skips): new `lib/sandbox.py` in all three suites deploys a sandbox instance on a free port via the MCP server's `sandbox.deploy`, then `stop`+`delete` in a `finally`. `conftest.py` gained `mariadb_server` (session-scoped) in front of `mariadb_connection`; any of `MARIADB_HOST/PORT/USER/PASSWORD` set → use that server instead (**keeps CI's service container working unchanged**), unreachable → skip as before. Default run is now **607 passed, 0 skipped ×3** (was 592 + 15 skipped), coverage 82%.
  - README updated (plugin table, "what each plugin provides", repo-layout tree, skills-sync section) for the subfolders + dev/sql split.
  - REST e2e split into 6 step tests + teardown-on-timeout fix. **Full REST e2e: 6 passed in 5m10s** vs local claude, incl. `test_step5` (REST DDL executed via MCP → `/notesApp` + endpoints verified) — works end-to-end **after the user fixed the mrs plugin**.
  - **MSM e2e: 6 passed in 11m23s** vs local claude (`test_e2e_msm_claude.py`) — claude scaffolded the `notes_app` project, authored v1.0.0 (tables→140, activity VIEW→150), released it, developed v1.1.0 (notebook/tag tables + notes_details VIEW), **filled the 1.0.0→1.1.0 update script** (240/250), and generated a v1.1.0 deployment script containing all objects.
  - **Test counts (all three suites, identical):** `-m static` → **602 passed**; the default tiers (static + db) → **618 passed, 0 skipped**; `-m e2e` → **12 passed** (claude only). Counts rose by 11 with `test_additional_skill_matches_its_source` (one per `additional` skill). The only hardcoded count is `test_expected_statement_skill_count == 31` (statement layer only; unaffected by the `additional` layer and by the connector split).
  - **Pi/pi.dev dev plugin**: repo-root `package.json` (`pi` field → `pi/dev-plugin/`, + `pi-mcp-adapter` dep), and under `pi/dev-plugin/`: `src/index.ts` extension, `scripts/setup-pi-mcp.sh` (MCP registration; verified idempotent + merge-preserving), reused `mariadb-mcp-launcher.{sh,cmd}`, README/CHANGELOG/LICENSE, skills vendored (75 after the 2026-08-10 re-sync). `.gitignore` gets `node_modules/`+`package-lock.json`; main README repo-tree gains root `package.json` + `pi/` entries. Existing 6 plugins' `skills-source.json` date-churn from the re-sync was reverted to keep the change focused.
  - **Launcher rewrite verified end-to-end** (macOS, against the real private repo, installing into temp dirs so `~/.local` stayed clean): all 4 resolution steps, both failure paths, a real prerelease install of `v26.8.0` with checksum verification, and an MCP `initialize` handshake answered by the installed binary. The `.cmd` is reviewed but **never executed** (no Windows host / no `pwsh` here) — that's the one untested surface.
  - **Version bump to 26.8.0** in 31 files via `scripts/set-mariadb-shell-version.sh`, which was itself fixed: it never matched the `*_disabled` launchers its own header claimed to cover, didn't know about `pi/`, and couldn't see `setup-pi-mcp.sh`'s `SHELL_VERSION="…"` shape.
  - **`.cmd` files are CRLF + ASCII-only**, pinned by a new repo-root `.gitattributes` (`*.cmd`, `*.cmd_disabled` → `eol=crlf`). LF-only batch files mis-parse `goto` labels and `^` continuations, both of which the new script leans on heavily.
  - **Docs**: launcher behaviour rewritten in all 7 MCP-bearing plugin READMEs + the main README; `## [Unreleased]` CHANGELOG entries added to those 7 plugins; new `cspell.json` ignoreWords; repo-root **`LICENSE`** (byte-identical GPLv2 copy of the plugins') + main README License section rewritten by the user.
  - **Main README restructured** (user-driven): intro reworded, table column `Agent` → `Harness`, **Installation moved directly under the harness table** with the `> NOTE:` token/prerelease caveat carried to the top of it, displaced overview content given a new `## Plugin variants` heading, and a "configure the MCP server" step (`mariadb-shell -- mcp setup`) added and propagated to all 7 plugin READMEs as `### Configure what the server may access`.
  - **pi-mcp-adapter claim corrected**: `pi install npm:pi-mcp-adapter` *is* still required; the READMEs no longer imply the repo install pulls it in (see Gotchas).
  - **Install-facing org is now `mariadb`** (a tracking fork of this repo): the 11 install commands in 9 READMEs (`/plugin marketplace add mariadb/ai-plugins`, `pi install git:github.com/mariadb/ai-plugins`) plus `homepage`/`repository` in `package.json` and the 6 `plugin.json` manifests. **Deliberately still `mariadb-corporation`**: the launcher's `MARIADB_SHELL_REPO` + install.sh/ps1 URLs (different repo), every skill-source reference (`sync-skills.sh`, the 7 `skills-source.json`, the READMEs' skills sections, the topical license link, contributor's `mariadb-shell/.claude/skills/`), and `additional-skills/README.md`. The 2 marketplace manifests need nothing — `"name": "mariadb"` + relative `source` paths resolve from whichever org hosts the clone; OpenCode names no org at all (local paths).
  - **Plugin package version → 26.8.0** via `scripts/set-plugin-version.sh`, itself fixed the same way its sibling was: it globbed only `claude|codex|opencode/*-plugin`, so **pi was missed** — `pi/*-plugin` added *and* the repo-root `package.json` appended explicitly (pi has no per-plugin manifest; the root file IS its manifest). Its JSON substitution is now **anchored to line start and no longer `/g`**, because `package.json` also carries dependency version ranges. 15 files → **17**. NB the two versions are **independent** — two unrelated things, two different scripts, and neither should ever be inferred from the other. They have both diverged (package 26.9.0 vs floor 26.8.1) and coincided (at 26.8.0, and **again at 26.9.0** since PR #10 moved the floor there); coinciding is a coincidence, never a rule.
  - **Re-synced skills (2026-08-14)**: dev/sql `870469e` → **`ec1d988`**, contributor `4c361e5` → **`af86380`** (the script finds `gh auth token` itself, so the private contributor tree synced too). Counts unchanged (75 dev / 47 sql / 1 contributor); 432 files, +883/−126, and the diff is **three separate things**: (a) every upstream skill gained a `<sub>_This page is: Copyright © 2026 MariaDB..._</sub>` footer — 394 of the insertions, mechanical; (b) the contributor `create-shell-plugin` skill was **rebranded upstream** (`mysqlsh` → `mariadb-shell`, `~/.mariadb-shell/plugins`, `globals.dba` → `globals.db`); (c) **`mariadb-schema-create-script`'s vendored copies were stale** — its `additional-skills/sql/` source was edited in `7a52cc3` (2026-08-10) with no sync afterwards, so all 7 plugins had been shipping the old text.
  - **New static test `test_additional_skill_matches_its_source`** (×3 suites, 11 params) closes exactly that hole: each vendored `additional`-layer skill must be byte-identical to its `additional-skills/*/<name>/SKILL.md` source. `lib/skills.py` gained `additional_skills()` + `additional_sources(name)` (lookup is **by name** — the manifest records the layer but not the `sql`/`rest`/`schema-management` subfolder; returns a *list*, since the same name in two subfolders is itself a fault). Three distinct failure messages (no source / >1 source / content differs → "re-run scripts/sync-skills.sh"). **Verified it actually fails** by perturbing a vendored copy. Only `additional-skills/` is covered — upstream-sourced skills have no local source to compare against.
  - **GPL-2.0 copyright header on all 46 source files** (+ `SECURITY.md` added): 23 Python modules above the docstring (keeps `__doc__` intact), `run_tests.py` + 11 shell scripts after the shebang (merged into the existing comment block), 3 empty `lib/__init__.py` header-only, 7 batch launchers as `rem` **after `@echo off`**, `pi/dev-plugin/src/index.ts` as `//`. Block = the MariaDB shell plugins' one **minus the `SPDX-License-Identifier` line and its `#` spacer** (user's call). Pure insertion: +696 / −0. YAML (`.github/workflows/*.yml`, `docker-compose.yml`), `requirements.txt` and the JSON manifests have **no** header.
  - **`wip/mcpLauncherUpdate`**: (1) the prerelease fallback above — verified live: with the shell off `PATH` and nothing set, the launcher reported "no stable release to install; retrying with --pre-release" and installed v26.8.0; `=0` refuses and fails stable-only. (2) **OpenCode's launchers enabled** — they shipped as `*_disabled` while `opencode.json` already pointed at `mariadb-mcp-launcher.sh`, so its MCP server could never have started; `git mv`'d, `+x` restored, all 14 copies identical again, `_disabled` handling dropped from `.gitattributes` and the version script. (3) The main README's "MCP server configuration/loading scripts are currently disabled" NOTE removed and all 7 plugin READMEs' "Prereleases" notes rewritten. (4) **`setup-codex-mcp.cmd`** for both codex plugins + a Windows note in the `.sh`.
  - **`scripts/set-mariadb-shell-version.sh` reach fixed again** — it never listed `setup-codex-mcp.{sh,cmd}`, both of which carry a `26.8.0` default, so a codex bump would have skipped them. Now **35** files (was 31). Third time this script's reach has lagged new files (pi, `_disabled`, now the setup scripts) — check it whenever a file gains a version site.
  - **DevHub shipped on `wip/getting-started-docs` (PR #12, open).** 9 tutorials covering the working example (schema + sandbox), `db.*`, schema browsing, `sandbox.*`, `EXPLAIN`, the MSM lifecycle over two releases, REST endpoints and the `migrator.*` flow. Content taken from the code, not from memory: tool signatures from `mcp_plugin/lib/`, workflows from the e2e suites' prompts, behaviour notes from the skills and this file's gotchas. Verified: clean build, every internal link and anchor resolves, all external URLs 200, **no horizontal overflow at 320/360/390/768/1024/1440px on every page**, and the filters, mobile nav and TOC driven in a real browser rather than eyeballed. Also removed six `.DS_Store` files (three tracked) and gitignored them.
  - **DevHub merged and live.** PR #12 landed (`ed6fd43`); since then #14 (sandbox/versions tutorial), #15 (titled code block as one panel), the CNAME + top-level `baseurl`, two logo fixes, **release 26.9.2** (#16) and an MSM e2e fix (#17). Counts now `skill_count: 82`, `tool_count: 28`, `shell_floor: 26.9.2`, 10 tutorials.
- **DevHub polish — merged as PR #18** (squash `054e1a5`, 3 files, +152/−6):
  - **`MCP` nav link** → `docs/_data/nav.yml`, placed after Skills, pointing at `/mcp-tools/` (the page's own `permalink`). Verified: renders on every page, active-state fires only there, no overflow, 86px of clearance left beside the icon buttons at 901px (the narrowest desktop nav).
  - **`.btn--primary` re-skinned** from mariadb.com's `btn-deep-ocean btn-outline-gradient` — deep-ocean `#0e2833` fill inside a 3px sublime-gradient ring, flooding to the full gradient with a deep-ocean label on hover/focus/active, plus the reference's focus halo and pressed inset shadow. **Crossfades rather than snapping** (a `::before` overlay fading its opacity, because `background-image` is not animatable — the reference itself snaps). Tokens `--ch-deep-ocean` + `--ch-deep-ocean-focus-rgb`, shared by both themes like the brand red.
  - **Hero code panels: inset collapses on narrow panels** — `@container (max-width: 480px) { .hero-panel__body { margin-inline: 0 } }`. Fully fixes the clipping at 1100px and nearly at 1024px; on a phone it only recovers ~19px of ~132px, so **the phone cut-off is NOT solved** (see Next steps).
  - **Scroll affordance on the hero code** — the code fades at the right edge and a `…` sits in the margin while more is hidden, both clearing at the end. Needed moving `overflow-x` from `.hero-panel__body` to `.hero-code`, since an overlay inside a scroll container scrolls away with the content. `site.js` toggles `.is-clipped` from a `scroll` listener + `ResizeObserver`, with an 8px slack so a sub-character sliver raises no marker. Progressive enhancement: JS off → scrolls unmarked.
  - Verified across 320/360/375/390/430/560/768/901/960/1024/1100/1200/1440px: marker correct at start/middle/end of scroll on both panels, no JS errors on pages with no hero panels, and page-level horizontal overflow still 0 on all 7 pages.
  - **Open question raised on the PR:** `--ch-accent-btn-hover` now has **no consumer** — the old `.btn--primary:hover` was its only one. Left in place deliberately, because the `--ch-*` palette is documented as a verbatim copy of the reference's and pruning one token makes the next palette diff noisier. `--ch-accent-btn` is still live (`.btn--outline:hover`).
- **Release 26.9.4 — IN PROGRESS** on `wip/26.9.4`, cut with the new `/release` command (PR #25, `5d07abf`); release PR about to open:
  - **Both versions to 26.9.4**: `set-mariadb-shell-version.sh` (33 files) and `set-plugin-version.sh` (17 files). mariadb-shell `v26.9.4` was published 2026-09-23 and is the newest release. **The "31 files" recorded for 26.9.3 was a miscount** — 26.9.3's commit touched the same 33 files; compare with `git show --name-only`, not `--stat`, which truncates long paths.
  - **Re-vendor again provenance-only**: `mariadb-docs` `1a89bd3` → `bb6522c`, `mariadb-shell` `291bf84` → `8adaada`; only the ten `skills-source.json` moved. Counts hold at 82 / 47 / 2.
  - CHANGELOG `[26.9.4]` in all ten plugins, modelled on each file's own 26.9.3 section. `./run_tests.py -m static`: 667 passed per suite, all four suites green.
  - Still to do after the merge: tag `v26.9.4`, prereleases on both repos, fork sync (keep `docs/CNAME` = `.org`).
- **Release 26.9.3 — SHIPPED** (PR #20, squash `b6ed467`; annotated tag `v26.9.3` = object `a38774d`, prerelease on both repos):
  - **Both versions to 26.9.3**, by the two scripts: `set-mariadb-shell-version.sh` (31 files — launcher fallbacks, `.mcp.json`/`opencode.json`, the `SHELL_VERSION=` lines in `setup-codex-mcp.{sh,cmd}` and `setup-pi-mcp.sh`, the README JSON snippets and their one-line `(default ...)` prose, and `shell_floor` in `docs/_config.yml`) and `set-plugin-version.sh` (17 files — every `plugin.json` `"version"`, every README `Version **x.y.z**`, and the repo-root `package.json`). mariadb-shell 26.9.3 was already released before the bump.
  - **The skills re-vendor brought no skill changes**: upstream `mariadb-docs` main moved `0e7e033` → `1a89bd3` and the contributor source `1d859c7` → `291bf84`, but no vendored skill content changed — only the ten `skills-source.json` provenance files. Counts hold at 82 dev / 47 sql / 2 contributor. Worth knowing the sync can be a no-op: check `git status` before assuming a re-vendor shipped anything.
  - **CHANGELOG entries added for 26.9.3 *and* 26.9.2** across all ten plugins. 26.9.2 had shipped with none, so the files jumped 26.9.1 → 26.9.3; its entry was reconstructed from `d5e2bfc` rather than invented (the migrator split 76 → 82, the re-vendor at `0e7e033`, both version bumps). Per-variant wording differs as it always has: dev carries the skill detail, sql says the migrator split is dev-only and stays at 47, contributor notes the `MARIADB_SHELL_VERSION` floor does not apply to it.
  - **Logo SVGs re-exported** from Affinity Studio (`serif:` → `affinity:` namespace, rescaled group transforms, the MariaDB wordmark now a named `MariaDB-Logo-Vertical` group of per-glyph paths). Found already modified in the working tree, unrelated to the release, so kept as its own commit.
- **Earlier, done:** the pi suite on `wip/piTests`. (pi harness is dev only; sql/contributor pi variants not yet created, and pi has no test suite — `run_tests.py` will pick one up automatically once `pi/dev-plugin-test*/conftest.py` exists.)
- **CI left alone on purpose:** `.github/workflows/*-test.yml` (now four, incl. `pi-test.yml` — static + db only, no eval job) still use `setup-python` + a per-suite `pytest`; GitHub runners have no `mariadb-shell`, so the shell-Python runner is a local/dev-machine path only. The per-suite `pyproject.toml` files stay authoritative for that.
- **Known issues:** none blocking.

