# Test suite dependencies for the MariaDB plugin for Pi (pi.dev).
# The plugin runtime itself stays dependency-free; these are dev/test only.

pytest>=8.0
pytest-cov>=2.10.1    # coverage reports for ../../run_tests.py
PyYAML>=6.0          # parse SKILL.md frontmatter and fixture YAML
PyMySQL>=1.1         # pure-Python MariaDB/MySQL client for the live-DB tier

# No LLM SDK here: pi drives whichever provider it is configured for, so the
# tier-4 e2e shells out to the `pi` CLI rather than calling a vendor API itself.
